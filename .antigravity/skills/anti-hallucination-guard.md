# ANTI-HALLUCINATION & COGNITIVE GUARD FOR ANTIGRAVITY

## 1. ATTENTION RULE
Before updating or creating any file, you MUST read the blueprint associated with the module (`./.antigravity/blueprints/product-module.md`).

## 2. STRICT PAYLOAD COMPATIBILITY
- When refactoring child tab components, you MUST inspect the parent orchestrator form to verify that your key names match the parent state payload exactly.
- DO NOT invent or assume database columns (e.g., `original_price`). Check the exact schema definitions or existing migration files first.

## 3. EXECUTION MANDATE
- You are forbidden from leaving placeholder comments like "// todo: insert code here".
- Always output completed, production-ready files.